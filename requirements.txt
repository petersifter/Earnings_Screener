# Pinned deliberately.
#
# These were unpinned, so every cloud run installed whatever was newest that
# day while the local venv kept an older, working set. yfinance shipped 1.5.2
# on 2026-07-23; a screener that works locally and returns nothing in the
# cloud is the classic symptom of exactly that gap.
#
# To match a known-good local venv instead, run:  pip show yfinance
# and change the version below to match.
yfinance==1.5.2
finance-calendars==0.0.7
pandas>=2.2,<3.1
curl_cffi>=0.15
